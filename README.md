# 💀 THE WASHHOUSE: AI LOG REFINERY

**MISSION:** To act as a digital washroom for raw AI exports (ChatGPT, Claude, Gemini). Extract high-fidelity, verbatim conversation logs and strip out machine-readable noise.

---

## 🏗️ THE STACK
- **BACKEND (THE BRAIN):** FastAPI (Python) + ijson iterative streaming.
- **FRONTEND (THE BODY):** Next.js 15 + Tailwind CSS + Framer Motion.
- **PROTOCOL:** Play A (Last Child Deterministic Strategy).

---

## 🚀 THE LAUNCH RITUAL

### 1. INITIALIZE THE BRAIN (BACKEND)
```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python main.py
```
*The engine will activate on `http://localhost:8000`.*

### 2. INITIALIZE THE BODY (FRONTEND)
```bash
cd frontend
npm install
npm run dev
```
*The Command Deck will flare at `http://localhost:3000`.*

---

## 🛠️ TACTICAL PROTOCOLS
1. **BREACH:** Drop your `conversations.json` into the central intake zone.
2. **CALIBRATION:** Armed your toggles (USER_INPUT, BOT_RESPONSE, INTERNAL_THOUGHTS).
3. **STRIKE:** Hit `INITIATE_CLEAN_SWEEP` to begin the refinery.
4. **EXTRACTION:** Download the compressed ZIP payload and witness the OMERTA_PROTOCOL purge.

**LA UNICA COSA:** High-density, kinetic, and verbatim. Real recognizes real.
